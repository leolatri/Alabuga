import React from 'react';
import ReactDOM from 'react-dom/client';
import AlabugaApp from './App.tsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AlabugaApp />
  </React.StrictMode>
);
